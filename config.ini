[Settings]
hotkey = ctrl+shift+d
library_dir = C:\Users\Noahs\My Drive\computers\Kicad_Bits
symbol_lib = Legacy_Parts
footprint_lib = Legacy_Parts

# --- Explanations ---
# hotkey: The keyboard shortcut to activate the background importer.
# library_dir: The absolute folder path where footprints, 3D models, and the symbol library will be saved.
# symbol_lib: The target library name to insert the symbol into.
# footprint_lib: The target library name to insert the footprint into.
